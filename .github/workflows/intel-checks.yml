name: Intelligence Checks

on:
  pull_request:
    branches: [ Beta, main ]
  workflow_dispatch:

jobs:
  intelligence-suite:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v6

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 24

      - name: Install pnpm
        uses: pnpm/action-setup@v5
        with:
          version: 10
          run_install: false

      - name: Get pnpm store directory
        shell: bash
        run: |
          echo "STORE_PATH=$(pnpm store path --silent)" >> $GITHUB_ENV

      - name: Setup pnpm cache
        uses: actions/cache@v4
        with:
          path: ${{ env.STORE_PATH }}
          key: ${{ runner.os }}-pnpm-store-${{ hashFiles('**/pnpm-lock.yaml') }}
          restore-keys: |
            ${{ runner.os }}-pnpm-store-

      - name: Install Dependencies
        run: pnpm install --no-frozen-lockfile

      - name: Structural Search (ast-grep)
        run: npx ast-grep scan --config .github/sgconfig.yml

      - name: Architectural Guard (dependency-cruiser)
        run: |
          npx depcruise --config .github/.dependency-cruiser.mjs Frontend-PWA/src
